import React from 'react'
import { Button } from 'antd'
import './index.less'

const Bold: React.FC = () => {
  return (
    <Button className="bold-btn" size="small" type="text">
      B
    </Button>
  )
}

export default Bold
