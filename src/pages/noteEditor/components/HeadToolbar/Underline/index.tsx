import React from 'react'
import { Button } from 'antd'
import './index.less'

const Underline: React.FC = () => {
  return (
    <Button className="underline-btn" size="small" type="text">
      U
    </Button>
  )
}

export default Underline
