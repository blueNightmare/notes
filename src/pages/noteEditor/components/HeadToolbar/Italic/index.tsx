import React from 'react'
import { Button } from 'antd'
import './index.less'

const Italic: React.FC = () => {
  return (
    <Button className="italic-btn" size="small" type="text">
      I
    </Button>
  )
}

export default Italic
